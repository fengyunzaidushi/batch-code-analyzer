# Milestone 01：首轮并行模块

只有 Milestone 00 全部通过后，才创建本里程碑的 worktree。

建议首轮并行不超过 5 个：

1. Domain + Repository；
2. Scanner + Security；
3. API Profile + Provider；
4. Frontend Shell；
5. Mock Provider + Integration Tests。

数据库 Migration、公共 IPC、错误码、Workspace 依赖和锁文件仍由单一 Owner 管理。
