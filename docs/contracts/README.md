# 公共契约

本目录保存跨模块共享且需要稳定版本控制的契约。

修改规则：

1. 契约修改必须先于实现修改；
2. 破坏性修改必须新增或更新 ADR；
3. IPC、状态枚举和错误码应由 Rust 单一来源生成 TypeScript 类型；
4. 普通模块 Agent 不得自行修改本目录；
5. 所有契约都应包含 `schemaVersion` 或明确的兼容策略。

当前契约处于 **v0.1 设计锁定阶段**。工程初始化 Agent 可以补充具体 Rust/TypeScript 类型，但不得改变 PRD 与架构定义的行为。
