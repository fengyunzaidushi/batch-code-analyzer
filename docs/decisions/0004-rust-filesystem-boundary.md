# ADR-0004：仓库文件系统只由 Rust 访问

- 状态：Accepted
- 日期：2026-07-15

## 背景

前端直接获得任意文件系统权限会扩大攻击面，也难以统一处理符号链接、路径逃逸、编码、秘密检测和跨平台路径差异。

## 决策

React 只能通过受控 Tauri Command 请求项目、文件摘要和结果。仓库遍历、文件读取、哈希、敏感检测和输出写入全部由 Rust 完成。

## 后果

- IPC DTO 需要更完整；
- 大结果必须分页或按需读取；
- 安全和路径逻辑集中在一个可测试的核心层。
