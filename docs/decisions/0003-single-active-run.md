# ADR-0003：首期只允许一个活动 Run

- 状态：Accepted
- 日期：2026-07-15

## 背景

多个项目、多个 API 档案和多批次同时执行会显著增加全局并发、限流、取消、恢复和 UI 状态复杂度。

## 决策

首期客户端任一时刻只允许一个 `Running/Pausing/Paused/Cancelling` Run。其他项目可以浏览、扫描和配置，但不能创建第二个活动 Run。

## 后果

- 调度器和恢复模型明显简化；
- 用户无法同时分析多个项目；
- 数据结构仍保留未来支持多活动 Run 的扩展空间。
